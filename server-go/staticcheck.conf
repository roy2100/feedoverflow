# staticcheck config — mirrors the JS-side oxlint policy: catch bugs, not style.
# Keep the bug/quality checks (SA*, S*, U*, QF*) and drop the whole ST* style
# group (e.g. ST1005 error-string capitalization) — style is gofmt's job.
checks = ["all", "-ST1*"]
