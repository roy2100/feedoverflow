// Package auth holds session login/logout, the per-request gate, the login
// rate-limit, and timing-safe credential compare. Port of server/auth.ts.
//
// Phase 0: skeleton only — auth + the two-listener model is Phase 4.
package auth
